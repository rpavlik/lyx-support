install(FILES vgtc.cls
	DESTINATION "${LATEX_CLS_DIR}/vgtc/")

install(FILES vgtc.layout
	DESTINATION "${LYX_LAYOUTS_DIR}")
	
install(FILES "IEEE VGTC Conference Paper.lyx"
	DESTINATION "${LYX_TEMPLATES_DIR}")
